export class Inspection {
      inspectionId: number;
      inspectionLocation: string;

    constructor(inspectionId: number, inspectionLocation: string) {
      this.inspectionId = inspectionId;
      this.inspectionLocation = inspectionLocation;
    }
  }